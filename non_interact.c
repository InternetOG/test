#include <shell.h>

int main(void)
{
        // Supress unsed parameter flag on compiler
        (void) argc;

        char **current_command = NULL;
        int i, type_command = 0;
        size_t n = 0;
}
